[project]
name = "falldap"
version = "0.1.0"
description = ""
authors = [{ name = "octonezd" }]
requires-python = ">=3.14,<4.0"
dependencies = [
    "flask (>=3.1.3,<4.0.0)",
    "ldaptor (>=21.2.0,<22.0.0)",
    "twisted (>=26.4.0,<27.0.0)",
    "flask-oidc (>=2.4.0,<3.0.0)",
    "python-dotenv (>=1.2.3,<2.0.0)",
    "scim2-models (>=0.6.12,<0.7.0)",
    "sqlmodel (>=0.0.39,<0.0.40)",
    "valkey (>=6.1.1,<7.0.0)",
    "valkey[libvalkey] (>=6.1.1,<7.0.0)",
    "bcrypt (>=5.0.0,<6.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
